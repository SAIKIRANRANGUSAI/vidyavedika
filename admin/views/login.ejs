<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login | VidyaVedika</title>
    <style>
        /* Reset */
        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }

        body, html {
            height: 100%;
            background: #f4f6f8;
        }

        .container {
            width: 900px;
            max-width: 95%;
            height: 550px;
            display: flex;
            margin: 50px auto;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            border-radius: 12px;
            overflow: hidden;
            background: white;
        }

        /* Left branding section */
        .left {
            flex: 1;
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 30px;
            position: relative;
        }

        .left img {
            width: 140px;
            margin-bottom: 20px;
            animation: logoFade 1.5s ease-in-out;
        }

        .left h2 {
            font-size: 26px;
            margin-bottom: 10px;
            text-align: center;
        }

        .left p {
            text-align: center;
            font-size: 16px;
            line-height: 1.5;
            opacity: 0.9;
        }

        @keyframes logoFade {
            0% { opacity: 0; transform: scale(0.8); }
            100% { opacity: 1; transform: scale(1); }
        }

        /* Right login form */
        .right {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px;
        }

        form {
            width: 100%;
            max-width: 350px;
        }

        form h2 {
            margin-bottom: 30px;
            color: #333;
            text-align: center;
            font-size: 28px;
        }

        .input-group {
            position: relative;
            margin-bottom: 25px;
        }

        input {
            width: 100%;
            padding: 14px 15px;
            border-radius: 6px;
            border: 1px solid #ccc;
            font-size: 16px;
            outline: none;
            transition: all 0.3s;
        }

        input:focus {
            border-color: #007bff;
            box-shadow: 0 0 5px rgba(0,123,255,0.3);
        }

        label {
            position: absolute;
            top: 50%;
            left: 15px;
            transform: translateY(-50%);
            color: #aaa;
            pointer-events: none;
            transition: 0.3s;
        }

        input:focus + label,
        input:not(:placeholder-shown) + label {
            top: -10px;
            left: 10px;
            font-size: 12px;
            color: #007bff;
            background: white;
            padding: 0 5px;
        }

        button {
            width: 100%;
            padding: 14px;
            border: none;
            border-radius: 6px;
            background: #007bff;
            color: white;
            font-size: 16px;
            cursor: pointer;
            transition: background 0.3s;
        }

        button:hover {
            background: #0056b3;
        }

        /* Responsive */
        @media(max-width: 768px) {
            .container {
                flex-direction: column;
                height: auto;
            }
            .left, .right {
                flex: none;
                width: 100%;
            }
            .left {
                padding: 30px;
            }
            .right {
                padding: 30px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Left branding -->
        <div class="left">
            <img src="/admin/static/images/logo.png" alt="VidyaVedika Logo">
            <h2>Welcome to VidyaVedika Admin Panel</h2>
            <p>Securely manage your website content, courses, gallery, and contact data all in one place.</p>
        </div>

        <!-- Right login form -->
        <div class="right">
            <form action="/admin/login" method="POST">
                <h2>Admin Login</h2>
                
                <div class="input-group">
                    <input type="text" name="username" placeholder=" " required>
                    <label>Username</label>
                </div>
                
                <div class="input-group">
                    <input type="password" name="password" placeholder=" " required>
                    <label>Password</label>
                </div>
                
                <button type="submit">Login</button>
            </form>
        </div>
    </div>
</body>
</html>
